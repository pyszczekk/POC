close all;
clearvars;
clc;

img = imread('kwadraty.png');
edge = edge(img, 'Sobel');
figure(1)
subplot(1,2,1);
imshow(img);
title('kwadraty');
subplot(1,2,2);
imshow(edge);
title('krawedzie-sobel');

